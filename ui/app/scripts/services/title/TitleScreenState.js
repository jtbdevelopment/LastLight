'use strict';

angular.module('uiApp').factory('TitleScreenState',
    [function () {
        return {
            create: function () {
                //  TODO
                //this.game.state.start('Act1', true, false, 0, 0);
            }
        };
    }
    ]
);
